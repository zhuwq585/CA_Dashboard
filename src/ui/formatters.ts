import type { SessionStatus } from '../types.js';

export function formatStatus(_status: SessionStatus): string {
	return '';
}

export function formatRelativeTime(_epochMs: number | undefined): string {
	return '';
}
